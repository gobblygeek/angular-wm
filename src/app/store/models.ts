export interface NavLink {
  link: string;
  label: string;
}
export interface UserInfo {
  userid: string,
  name: String,
  level: number, 
  att: number, 
  def: number, 
  gold: number,
  inkpots: number
}